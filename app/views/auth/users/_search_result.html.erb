<div class="card">
	<div>Email: <%= result.email %> </div>
	<div>Mobile: <%= result.additional_login_param %> </div>
	<% if current_signed_in_resource.is_admin? %>
		<%= link_to "Set As Current User", main_app.set_proxy_resource_profiles_path(:resource => current_signed_in_resource.class.name.downcase.pluralize, :id => result.id.to_s), :remote => true, :method => :post %>
	<% end %>
</div>