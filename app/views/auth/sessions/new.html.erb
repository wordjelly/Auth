<!-- would make sense to have some script here to launch the modal and show the errors whatever they may be alongwith the flash. -->
<%= render :partial => 'auth/shared/devise_error_messages.html.erb', locals:{resource: resource} %>