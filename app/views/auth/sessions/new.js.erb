$("#login_title").html("Sign In").show();
//CONTENT
$("#login_content").html("<%= escape_javascript(render partial: 'auth/modals/login_default_content.html.erb') %>").show();
$("#login_forms").html("<%= escape_javascript(render partial: 'auth/modals/login_forms.html.erb', locals: {:resource => resource}) %>").show();
$("#recaptcha_tags").html("<%= escape_javascript(render partial: 'auth/modals/recaptcha_tags.html.erb') %>").show();
//FOOTER
$("#login_footer").html("<%= escape_javascript(render partial: 'auth/modals/login_navigation_options.html.erb') %>").show();
open_sign_in_modal_if_closed();
//apply the form validtion plugin here.

//the user login validation function is included in you_need_to_sign_in.js
var validation_settings = {
  "login_form" : {
    "user_login" : {
      "validation_events":{
        "keyup" : true
      },
      "validate_with":[
        {"required" : "true",
         "failure_message": "this field is required"
    	},
    	{"format" : user_login_validation_function,
    	 "failure_message": "Please enter a valid email or mobile number"
    	}
      ]
    },
    "user_password":{
    	"validation_events":{
    	  "keyup":true
    	},
    	"validate_with":[
      	{"required" : "true",
      	 "failure_message": "this field is required"
      	}
      ]
    }
  }
}

// Initialize the validator
try{
	var validator = new WJ_Validator(validation_settings,"materialize",true);
}
catch(err){
	console.log(err);
}