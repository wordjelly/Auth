<!-- provide an option to add all those cart items directly into a cart, so basically a form here to make a cart -->

<!-- now cart should accept a discount_id, since this will be sent into the payment to make a discount based payment. -->


<%= render :partial => "auth/shopping/carts/form" %>