<%= form_for(@auth_shopping_payment, url: payments_path) do |f| %>
  <% if @auth_shopping_payment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@auth_shopping_payment.errors.count, "error") %> prohibited this auth_shopping_payment from being saved:</h2>

      <ul>
      <% @auth_shopping_payment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
